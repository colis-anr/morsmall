(jbuild_version 1)

(copy_files ext/*)
(copy_files printer/*)

(library
 ((name        morsmall)
  (public_name morsmall)
  (preprocess (pps (ppx_deriving.std)))
  (libraries (libmorbig))
  (flags (:standard -w +A-4-30-42))))
